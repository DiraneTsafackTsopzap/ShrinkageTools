<div class="card shadow-sm border-0">
    <div class="card-header bg-white border-0 d-flex align-items-center justify-content-between">
        <div class="d-flex align-items-center gap-2">
            <span class="badge rounded-pill text-bg-primary">Activities</span>
            <h5 class="mb-0">Manage Activities</h5>
        </div>

        <button class="btn btn-sm btn-outline-primary" @onclick="OnRefresh">
            🔄 Refresh
        </button>
    </div>

    <div class="card-body">
        <p class="text-muted mb-3">
            Ici tu vas afficher la liste des activités, filtrer par date, ajouter / supprimer, etc.
        </p>

        <div class="alert alert-info mb-0">
            ✅ Component prêt. Ajoute ton tableau / formulaire ici.
        </div>
    </div>

    <div class="card-footer bg-white border-0 d-flex justify-content-end gap-2">
        <button class="btn btn-sm btn-outline-secondary" @onclick="OnReset">
            Reset
        </button>
        <button class="btn btn-sm btn-primary" @onclick="OnAdd">
            + Add activity
        </button>
    </div>
</div>

@code {
    private void OnRefresh()
    {
        // TODO: appeler ton API / store pour reload
        Console.WriteLine("Refresh clicked");
    }

    private void OnReset()
    {
        // TODO: reset filtres / sélection
        Console.WriteLine("Reset clicked");
    }

    private void OnAdd()
    {
        // TODO: ouvrir modal / formulaire
        Console.WriteLine("Add clicked");
    }
}
